import Settings from '@/components/settings/settings-component';
import React from 'react';

const Page = () => {
    return (
        <div>
            <Settings />
        </div>
    );
}

export default Page;
